/**
 * API Keys template.
 *
 * HOW TO USE:
 * 1. Copy this file to `apiKeys.js` in the same folder.
 * 2. Paste your real keys into apiKeys.js.
 * 3. apiKeys.js is gitignored - it will NEVER be committed to GitHub.
 *
 * ⚠️ SECURITY WARNING:
 * Putting a real Claude key directly in the app is OK for PERSONAL TESTING only.
 * For real users, the key must live on a backend (Cloud Function), never in the app.
 */

export const CLAUDE_API_KEY = 'YOUR_CLAUDE_API_KEY';

// Google OAuth Web Client ID (from Firebase Console > Auth > Google > Web SDK)
export const GOOGLE_WEB_CLIENT_ID = 'YOUR_WEB_CLIENT_ID.apps.googleusercontent.com';
