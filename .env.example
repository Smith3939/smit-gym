# Enables the secure Vercel AI route in web builds. This value is safe to expose.
EXPO_PUBLIC_AI_BACKEND_ENABLED=true

# Required only for iOS / Android builds. The web app uses /api/claude automatically.
# EXPO_PUBLIC_AI_API_URL=https://your-app.vercel.app/api/claude
